// constants.js

const ALLOW_PAGE = ['login', 'signup', 'forgot-password', 'error']

const NOT_SHOW_HEADER_PAGE = ['/login', '/signup', '/forgot-password', '/error']

module.exports = {
  ALLOW_PAGE,
  NOT_SHOW_HEADER_PAGE
}
